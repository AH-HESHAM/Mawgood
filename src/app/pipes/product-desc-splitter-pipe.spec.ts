import { ProductDescSplitterPipe } from './product-desc-splitter-pipe';

describe('ProductDescSplitterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductDescSplitterPipe();
    expect(pipe).toBeTruthy();
  });
});
